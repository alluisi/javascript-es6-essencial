// Operdor binário
// operando operador operando2
1 + 2

// Operador unário
// operando1 operador
// operador operando1
x++
++x

// Módulo (%)
// Operador binário. Retorna o inteiro restante da divisão dos dois operandos.

12 % 5 // retorna 2.

// Incremento (++) e Decremento (--)
++x
x++

const a = ++2; // Já recebe o valor incrementado
const b = 2++ // Recebe o valor atual

--x
x--

    // Subtração(-) e Adição(+)
    - 3
    + "3" // retorna 3
    + true // retorna 1
    + false // retorna 0
    - true // retorna -1

// Operador de exponenciação (**)
2 ** 3 // retorna 8
10 ** -1 // retorna 0.1

// Operador de agrupamento ()
2 * (3 + 2)